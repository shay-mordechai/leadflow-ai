# requirements.txt

# --- Core Framework & Server ---
fastapi
uvicorn[standard]
gunicorn
prettytable

# --- Database & Models ---
SQLAlchemy
psycopg2-binary
pydantic
pydantic-settings
boto3
alembic
python-dotenv

# --- UI & Utilities ---
jinja2
python-multipart
aiofiles
email-validator

# --- HTTP Clients & Networking ---
httpx
requests

# --- AI & Processing ---
# Note: faster-whisper is memory intensive (requires min 2GB RAM on EC2)
faster-whisper
openai>=1.0.0
google-generativeai

# --- Security & Auth ---
# Bleach: Required for HTML sanitization in input validation
bleach==6.1.0

# Cryptography: Core library for encryption operations
cryptography

# Password Hashing:
# Locked to 4.0.1 to prevent 'Invalid salt'/C-extension errors on Amazon Linux
bcrypt==4.0.1
passlib[bcrypt]

# JWT Tokens:
python-jose[cryptography]

# Rate Limiting:
slowapi

# --- Telephony Providers ---
twilio>=8.0.0
telnyx>=2.0.0
vonage