# .github/workflows/deploy.yml
# ==============================================================================
# ⚠️  CRITICAL CONFIGURATION WARNING ⚠️
# ==============================================================================
# Target Server OS: Amazon Linux 2023
# Container Runtime: PODMAN
# Architecture: Zero Trust (Cloudflare Tunnel)
#
# CRITICAL NOTE ON DEPLOYMENT LOGIC:
# Since SSH access is routed THROUGH the Cloudflare Tunnel container,
# we must NEVER run 'podman-compose down' on the entire stack.
# Doing so would kill the tunnel, sever the SSH connection, and fail the job.
# We only restart the 'app' service, keeping 'tunnel' alive.
# ==============================================================================

name: LeadFlow Production Deploy

on:
  push:
    branches: [ main ]

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: shay-mordechai/leadflow-ai

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Log in to the Container registry
        uses: docker/login-action@v2
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v4
        with:
          context: .
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}

  deploy:
    needs: build-and-push
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Install Cloudflared
        run: |
          curl -L --output cloudflared.deb https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64.deb
          sudo dpkg -i cloudflared.deb
          cloudflared --version

      - name: Configure SSH & Tunnel
        env:
          # מעבירים את המשתנים כ-ENV כדי למנוע בעיות ציטוט בפקודת ה-Proxy
          CF_ID: ${{ secrets.CF_SERVICE_ID }}
          CF_SECRET: ${{ secrets.CF_SERVICE_SECRET }}
        run: |
          mkdir -p ~/.ssh/
          echo "${{ secrets.EC2_SSH_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          
          # יצירת קובץ קונפיג עם נתיב מלא והשתקת לוגים
          cat > ~/.ssh/config <<EOF
          Host production
            HostName ${{ secrets.CF_SSH_HOSTNAME }}
            User ec2-user
            IdentityFile ~/.ssh/id_rsa
            StrictHostKeyChecking no
            UserKnownHostsFile /dev/null
            ProxyCommand /usr/local/bin/cloudflared access ssh --hostname %h --id $CF_ID --secret $CF_SECRET --no-autoupdate --loglevel error
          EOF

      - name: Create Podman Deployment Script
        # שינוי קריטי: הסרנו את הקוד שנוגע בטנל! הטנל מנוהל כעת ברמת השרת (Systemd) ולא צריך לגעת בו
        run: |
          cat <<EOF > deploy_script.sh
          #!/bin/bash
          set -e

          # Setup Variables
          NEW_TAG="${{ github.sha }}"
          APP_DIR=~/leadflow-ai

          # Ensure User Persistence
          sudo loginctl enable-linger ec2-user

          mkdir -p \$APP_DIR
          cd \$APP_DIR

          # 1. Fetch Secrets (App specific only)
          # (Optional: Add fetch for other secrets if needed)

          # 2. Login & Pull NEW version
          echo "${{ secrets.GITHUB_TOKEN }}" | podman login ghcr.io -u ${{ github.actor }} --password-stdin

          echo "⬇️ Pulling version: \$NEW_TAG"
          podman pull ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:\$NEW_TAG

          # 3. Deploy NEW Version
          echo "🚀 Deploying version: \$NEW_TAG"
          export IMAGE_TAG=\$NEW_TAG

          # ⚠️ CRITICAL: We only restart the APP, never the tunnel
          echo "🔄 Updating App Container..."
          podman stop leadflow-ai || true
          podman rm leadflow-ai || true

          # This brings up the app
          podman-compose up -d

          # 4. Ensure Systemd Persistence (Only for the App)
          echo "🛡️ Configuring Auto-Start for App..."
          mkdir -p ~/.config/systemd/user/
          cd ~/.config/systemd/user/

          podman generate systemd --new --files --name leadflow-ai
          
          systemctl --user daemon-reload
          systemctl --user enable container-leadflow-ai.service
          
          cd \$APP_DIR

          # 5. Health Check
          echo '--- 🏥 Health Analysis (Waiting 15s) ---'
          sleep 15

          CONTAINER_ID=\$(podman ps -q --filter "name=leadflow" | head -n 1)

          if [ -z "\$CONTAINER_ID" ]; then
             echo '❌ CRITICAL: No container found running!'
             exit 1
          else
             echo '✅ Deployment Successful. App is running.'
          fi

          # 6. Cleanup
          echo '--- 🧹 Cleaning up old images ---'
          podman image prune -f
          EOF

      - name: 🔍 Debug Connection
        run: |
          echo "Testing connection to Cloudflare..."
          HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" \
            -H "CF-Access-Client-Id: ${{ secrets.CF_SERVICE_ID }}" \
            -H "CF-Access-Client-Secret: ${{ secrets.CF_SERVICE_SECRET }}" \
            https://${{ secrets.CF_SSH_HOSTNAME }})

          echo "⚠️ SERVER RESPONSE CODE: $HTTP_CODE"

          if [ "$HTTP_CODE" -ne 200 ]; then
             echo "❌ Connection check failed. Stopping."
             exit 1
          fi

      - name: Execute Deployment on Server
        run: |
          # Copy file - using -v to debug if it fails
          echo "📂 Copying files..."
          scp -v docker-compose.prod.yml production:~/leadflow-ai/docker-compose.yml
          scp deploy_script.sh production:~/leadflow-ai/deploy.sh

          # Run script
          echo "🚀 Executing remote script..."
          ssh production "chmod +x ~/leadflow-ai/deploy.sh && ~/leadflow-ai/deploy.sh"
